---
- name: Déployer application Docker
  hosts: localhost
  # become: true
  vars:
    repo_dest: "/home/mathys/Infra-et-orch-de-donn-es"
  tasks:
    - name: Build et up Docker Compose
      command: docker compose up -d --build
      args:
        chdir: "{{ repo_dest }}"
